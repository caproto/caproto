from distutils.core import setup
import setuptools


setup(name='caproto',
      author='Daniel Allan',
      description='a sans-I/O implementation of the EPICS Channel Access '
                  'protocol',
      packages=['caproto'],
      )
